# Where the app is published: host + base path, no scheme.
# Drives every outward-facing link (Shortcuts, QR codes, bookmarks) in both
# https:// and webapp:// form. Change here when the app moves.
VITE_APP_HOST=ergi88.github.io/xpp
