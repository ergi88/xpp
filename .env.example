# Copy to .env.local to override per machine. Production values live in
# .env.production, which is committed.

# Host + base path this app is published at, without a scheme.
# Leave unset in development to use whatever localhost the dev server picks.
VITE_APP_HOST=ergi88.github.io/xpp
